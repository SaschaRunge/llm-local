package commands

/*
import (
	"reflect"

	"github.com/SaschaRunge/llm-local/internal/cli/commands/selector"
	"github.com/SaschaRunge/llm-local/internal/cli/selector"
	"github.com/SaschaRunge/llm-local/internal/core"
)

func editJson(structFromJson reflect.Value) reflect.Value {
	selector.Select(reflect.Fields(structFromJson), func(item reflect.Value) string {
		MapOf
	})

	/*
		for _, card := range reflect.VisibleFields(reflect.TypeOf(scenes.Card{})) {
				fmt.Printf("%s\n", card.Name)
			}
}

*/
